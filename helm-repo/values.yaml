replicaCount: 1

image:
  repository: ""    # pipeline will overwrite
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

ingress:
  enabled: true
  className: "nginx"
  hosts:
    - host: ""     # empty → works with ALB hostname
      paths:
        - path: /
          pathType: Prefix

resources: {}
